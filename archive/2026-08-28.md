# 2026-08-28 完成归档

## 已实现

- [x] 【BUG】同步设置中的 SecretKey 字段的小眼睛没有用，打开还是看不见——根因：后端回显刻意不含 secret（仅 `has_secret` 布尔），前端密文框恒为空、占位符 `••••••••` 伪装成掩码内容，眼睛切换空输入无效果。修复：配置回显逻辑下沉 `mosh-core sync::config::config_view` 并回传 `secret_key` 明文（本地单机与 access_key 同级凭据）；表单预填真实值、保存后回填，小眼睛即可查看；占位符改为「留空保持原密钥」消除伪装；附回归测试（已存 secret 必须能从回显拿到）
