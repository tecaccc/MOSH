# 侧栏隐藏按钮动画 + 首页移除未实现统计卡

## Goal

处理 TODO-List 两项：① 侧栏新增隐藏/展开按钮且动画平滑；② 首页移除未实现的
「待办笔记」「活跃项目」统计卡。

## Requirements

- 开关固定在标题栏最左侧，隐藏与展开为同一位置（用户返工要求）；
- 隐藏/展开必须带动画（宽度渐变 + 内容淡入淡出，禁止生硬切换）；
- 折叠状态重启后保持（localStorage）；
- 首页 Stats 只保留有真实数据的卡片。

## Acceptance Criteria

- [x] 标题栏最左固定开关：展开时 «，折叠后图标旋转 180° 变 »，位置不变；
- [x] 折叠/展开 0.28s 缓动动画，标题栏左段与侧栏同步收放，边线无缝；
- [x] Ctrl/⌘+B 可切换；≤900px 响应式（64px 图标栏）下同样成立；
- [x] 首页不再出现「待办笔记」「活跃项目」假数据卡，相关死代码清除；
- [x] `npm run check` + `npm run build` 通过。

## Notes

- 动画不依赖 grid-template-columns 过渡：首列 auto 跟随侧栏 min/max-width
  同步过渡（各 webview 通吃）。
- 详细记录见 `archive/2026-08-27.md`。
