# 更新日志

本文件记录 MOSH 面向用户的显著变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)。

发布流程见 `docs/release.md`：每次发版（推 `v*` 标签）前，在对应版本小节补充变更。

## [Unreleased]

## [0.5.1] - 2026-08-19

### 修复

- **同步后界面不刷新**：在其他设备上的修改（待办重构/子任务、设置、聊天会话等）同步到本机后已写入数据库，但界面仍显示启动时的旧数据，需重启应用才能看到。现在同步落地远端变更后会自动刷新：待办/日程列表立即更新，聊天会话侧栏实时出现他机新会话，正停留在同一会话时消息实时补入。纯推送（本地编辑后的自动上传）不触发刷新，避免无谓重载。
- **同步合并计数虚高**：合并统计把已存在的聊天消息也计入“已变更”，导致每次自动推送都被误判为有远端变更。现仅统计真实新落地的行，同步完成提示里的数字恢复真实。

## [0.5.0] - 2026-08-19

### 新增

- **待办事项页**：新增独立「待办」页（侧栏 → 待办），集中查看全部待办——进行中按「已逾期 / 今天到期 / 即将到来 / 无截止日期」分组（逾期红框高亮），已完成按完成时间倒序回看（可取消勾选恢复）；支持全部 / 进行中 / 已完成过滤与一键新建。此前已完成的历史待办只能在「今日」视图看到当天完成的，其余无处可寻。
- **邮件通知**：设置 → 通知，日程提醒与待办到期可选择通过邮箱送达（SMTP 直发）。支持系统通知/邮件通知双通道独立开关（可同时开启）；SMTP 服务器、端口与加密方式（STARTTLS 587 / SSL 465 / 不加密）可配，切换加密方式自动回填默认端口；授权码不回显、留空保存即保留原值；一键发送测试邮件验证配置；配置随多设备同步端到端加密同步。纯 Rust rustls 栈，无 OpenSSL 依赖。

### 修复

- **同步 NoSuchBucket 可读排障 + 桶域名容错**：同步/测试连接失败时不再倾倒原始 XML 错误，改为解析 S3 错误码并给出排查提示—— NoSuchBucket 提示腾讯云 COS 桶名需带 APPID 后缀、endpoint 地域须与桶所在地域一致；AccessDenied / InvalidAccessKeyId / SignatureDoesNotMatch 等常见码均有对应指引。同时修复：endpoint 填了完整桶域名（桶字段填短名，如 `mosh` vs `mosh-1250000000`）时会拼出双重前缀主机名导致必 404，现在直接采用该桶域名；桶不存在时 GET 404 不再被静默当作“远端无数据”。
- **MCP 服务器打开应用不显示**：技能与 MCP 列表此前仅在打开聊天视图后才加载，启动直接进「设置 → AI 工具」时列表与计数为空；现在应用启动即拉取，已配置的服务器即刻可见。

## [0.4.0] - 2026-08-18

### 新增

- **多设备同步**：设置 → 同步，通过 S3 兼容对象存储（腾讯云 COS / 阿里云 OSS / Cloudflare R2 / Backblaze B2，亦支持 MinIO 等自建网关）在多台电脑间同步待办、日程、设置与 AI 会话历史。端到端加密（AES-256-GCM，云端只见密文，密钥仅存本机）；同步为事件驱动（启动拉取、变更 5 秒后自动推送、退出兑底），无任何轮询，空闲时零网络请求；支持虚拟主机式/路径式寻址、请求超时与 TLS 校验开关、连接测试；默认关闭，需手动启用。详见 `docs/sync-design.md`。
- **全局 Toast 通知**：保存成功/失败、测试连接、后台同步失败等操作反馈统一为顶部居中弹出的半透明卡片（自动消失；同步失败附「重试」按钮；恢复后自动收起），不再散落在各面板内联或屏幕角落。
- **MCP 工具详情**：设置 → 工具的「测试连接」成功后展开工具列表，逐个展示工具名、用途描述与入参概览（必填项标 `*`），无需翻阅服务器文档即可了解可用工具。

### 改进

- 同步设置表单改为逐行布局，密钥输入框支持显示/隐藏切换，密钥展示改为卡片式。
- 网络错误信息展开底层原因链（如 DNS 解析失败、连接超时），不再只显示一层笼统的“请求失败”。

### 修复

- 腾讯云 COS 同步 403：裸地域域名 endpoint 自动补 `cos.` 前缀，未填 region 时从 endpoint 提取签名地域，不再因空 region 签名失败。

## [0.3.1] - 2026-08-17

### 修复

- 单实例运行：重复点击桌面图标不再创建多个后台进程——已有实例在跑时，新进程立即退出并唤起旧实例主窗口（后台驻留模式下，驻留托盘的实例被顶到前台）。

## [0.3.0] - 2026-08-17

### 新增

- **个人资料**：设置 → 个人资料可配置名称与头像（上传图片或表情），首页/今日问候、侧栏顶部展示；不再硬编码默认名称。
- **关闭按钮行为可配置**（设置 → 通用）：可选「直接退出」或「后台驻留」；后台驻留时窗口隐藏到系统托盘，待办/日程提醒照常推送，左键点击托盘图标恢复窗口（Linux 需托盘扩展支持，不可用时自动回退直接退出）。
- **数据目录可自定义**：编辑系统配置目录下的 `config.toml` 的 `data_dir`（支持 `~` 前缀）即可迁移数据库位置；优先级 环境变量 `MOSH_DATA_DIR` > 配置文件 > 系统默认；设置 → 关于可查看并一键打开。详见 README「数据目录自定义」。
- **待办日期语义**：待办改为「创建日期 + 可选截止日期」，支持跨天/无截止事项；列表行展示创建日与截止（逾期红显），编辑器显示创建时间。

### 改进

- 滚动条：去掉轨道框，仅保留滑块，且只在滚动进行时显示。
- 提示弹出：通知卡片改为从顶部向下弹出，多卡片纵向堆叠。
- 待办/日程提醒仅使用系统通知（应用内弹窗已移除，避免无法关闭的堆积）。

### 修复

- AI 助手创建/修改/删除待办与日程后，首页/今日/日历视图立即刷新，无需重启。
- 发布流水线并行写更新清单（latest.json）的竞态：Windows 端「检查更新」曾报 *None of the fallback platforms … were found*；改为构建完成后单点生成清单。
- 侧栏顶部展示用户头像与名称（未配置时保持 MOSH 标识）。

## [0.2.0] - 2026-08-17

### 新增

- **系统通知**：日程提醒与待办到期除应用内弹窗外，同时经操作系统通知中心提醒（Windows / macOS / Linux）；待办到达截止时间后 1 小时内提醒一次，错过不补发。

### 修复

- AI 助手创建 / 修改 / 删除待办与日程后，首页、今日、日历等视图立即刷新，不再需要重启应用才能看到。
- 发布流水线并行写更新清单（`latest.json`）的竞态：v0.1.1 曾因此丢失 Windows 条目，导致 Windows 端「检查更新」报 *None of the fallback platforms … were found*；现改为全部平台构建完成后单独生成并上传清单，并附带修复存量 Release 的脚本（`scripts/gen-updater-json.py`）。

## [0.1.1] - 2026-08-16

### 新增

- 应用标识图标：紫渐变方块 + 白色 M + 绿色对勾角标，覆盖 Windows / macOS / Linux 安装包与浏览器 favicon；源图与重生成脚本入库（`npm run icon`）。
- 本更新日志（CHANGELOG.md）。

## [0.1.0] - 2026-08-16

首个发布版本。

### 新增

- **待办**：创建 / 编辑 / 软删（数据保留于库）、子任务（一层嵌套）、优先级、截止时间、完成状态；完成时自动记录完成时间点，恢复为进行中自动清除。
- **日程（日历）**：月 / 周 / 日 / 议程四视图；定时与全天事件；周期重复（daily / weekly / monthly / yearly）；提前提醒（toast 通知）；农历与节气显示。
- **首页仪表盘**：天气 + 时钟 + 问候 Banner、今日统计卡、日程安排（30 天按日分组）、待办事项卡、迷你月历。
- **天气**：Open-Meteo 按城市配置，坐标解析后持久复用。
- **AI 助手**：
  - OpenAI 兼容端点多提供商（DeepSeek / 通义 / Kimi / Ollama…），官方预置可改地址与模型，支持自定义；
  - 自然语言操作待办与日程（创建 / 查询 / 修改 / 删除），流式输出，工具调用卡片可展开查看输入与返回；
  - **Skills**：内置 + 自定义技能，启用后注入系统提示词；
  - **MCP**：接入 Streamable HTTP 外部工具服务器；
  - **权限管理**：工具审批三模式（免审批 / 写操作审批 / 全部审批）；
  - 会话管理：历史会话、删除、标题栏开关。
- **软件更新**：GitHub Actions 自动发布 Release（macOS 双架构 / Linux AppImage+deb / Windows NSIS 安装包）；应用内自动检测新版本、下载安装并重启（设置 → 关于 亦可手动检查）。

### 说明

- Windows 自本版本起分发 **NSIS 安装包**（`mosh_x.y.z_x64-setup.exe`，简中/英文向导，可选仅当前用户 / 所有用户安装），不再分发裸 `mosh.exe + DLL` 便携版。
- 数据落地本地 SQLite（`app_data_dir/mosh.sqlite`），不依赖任何云端服务。
